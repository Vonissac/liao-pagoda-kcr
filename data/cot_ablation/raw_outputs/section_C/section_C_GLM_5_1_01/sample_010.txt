**输入描述**：
目标古塔为朝阳南塔（灵感寺塔），据文献“凡一十三级”及辽金时期该地域建塔传统，判定为【密檐式八角砖塔】。文献提及“有千岁灯，以然于内。有百炼镜，以悬于外”，暗示塔身可能存在供灯的假门与悬镜的假窗交替布置，塔基壸门内应有灵瑞雕刻。

**推理过程**：
1. **类型与特征辨别**：文献明确“一十三级”，结合朝阳地区辽塔遗存（如朝阳北塔、云接寺塔等），该塔为典型的八角十三层密檐式砖塔。
2. **刚性覆盖与拓扑重组**：基于亲缘原型广胜寺塔（密檐式八角塔）的骨架进行拓扑重组。顶层特制塔檐为 `a`，后续需补齐12层密檐以满足“十三级”的刚性约束。密檐区采用辽金典型的 `b-h-c` 循环结构，最底层密檐加入斗栱 `d[type=d1]` 过渡。
3. **柔性渗透与差异化采样**：
   - **塔身突变**：针对“千岁灯（内）”与“百炼镜（外）”的描述，将塔身墙面设计为假门面（内点灯）与假窗面（外悬镜）径向交替，即双组括号并列 `(e8)(e11)`；转角柱采用普通柱 `[col=e1]`。
   - **塔基突变**：塔身下方设莲花座 `f`；壸门层参考辽代密檐塔常雕狮子、瑞兽等灵瑞动物，设定为每面2个壸门，间柱为普通柱，内雕动物，即 `g[num=2](e1-g9)`；最后过渡至地表 `i`。
4. **刚性拓扑约束验证链校验**：
   - **分界点判定**：全序列最后一个 `e` 为塔身，其左侧为檐区（无 $f, g, n$），右侧为基座区，校验通过。
   - **层数守恒计算**：塔檐区包含 1个 `a` 和 12个 `c`，1+12=13，严格等于文献“一十三级”，校验通过。
   - **壸门层合规**：`g[num=2](e1-g9)` 严格符合 `g[num=X](e[1-5]-g[8-12])` 格式，校验通过。
   - **首尾边界校验**：以 `Global(Sides=8,BaseR=10.0)+a` 起始，以 `-i` 绝对收尾，校验通过。

**标准 LPSL 编码**：
FINAL LPSL: Global(Sides=8,BaseR=10.0)+a-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-d[type=d1]-e[col=e1](e8)(e11)-f-g[num=2](e1-g9)-i