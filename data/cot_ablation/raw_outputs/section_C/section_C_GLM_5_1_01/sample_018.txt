*   **输入描述**：目标古塔为朝阳南塔（灵感寺塔），建于辽代太平九年。文献核心特征为“凡一十三级，通百有余尺”，且有“千岁灯以然于内，百炼镜以悬于外”的特殊功能描述。结合辽代柳城（今朝阳）地区古塔遗存特征，判定该塔为【八角密檐式砖塔】。

*   **推理过程**：
    1.  **类型与特征辨别**：文献明确指出“一十三级”，结合辽代密檐塔盛行的历史背景与朝阳地区现存古塔（如朝阳北塔）形制，判定为八角十三层密檐式砖塔。文献中“千岁灯”、“百炼镜”暗示塔身底层具有供燃灯与悬镜的孔洞或假窗/假门结构。
    2.  **刚性覆盖与拓扑重组**：绝对刚性特征为13层檐。参照密檐塔经典拓扑，顶层特制檐 `a`，后续12层标准密檐。循环体采用辽金典型的 `b-h-c`（束腰-素面-标准檐）结构，最底层檐下带斗栱 `d[type=d1]`。塔檐区序列重组为：`a` + 11次 `b-h-c` + 1次 `b-h-c-d[type=d1]`。
    3.  **柔性渗透与差异化采样**：
        *   *塔身柔性突变*：针对“千岁灯以然于内，百炼镜以悬于外”，在塔身 `e` 的二维网格中进行逻辑挂载。底层设假门 `e8`（灯内供）与假窗 `e11`（镜外悬），组合为 `e11-e8-e11`；上层辅以飞天 `e6` 与祥云 `e12`，中层设华盖 `e9`。转角柱采用普通柱 `[col=e1]`。最终塔身突变为：`e[col=e1](e6-e12-e6/e9-e9-e9/e11-e8-e11)`。
        *   *塔基柔性突变*：基于亲缘原型，塔基设莲花座 `f`，壸门层设每面2个，间柱普通柱 `e1`，内雕人物 `g8`（辽代常见），外侧带单层栏板 `k`，最终收束于地表 `i`。
    4.  **刚性拓扑约束验证链校验**：
        *   *分界点判定*：序列中最后一个 `e` 为塔身，其左侧全为檐区结构（无 f/g/n），右侧全为基座结构（无 a/c/r），合规。
        *   *层数守恒计算*：最后一个 `e` 左侧共有 1个 `a` 和 12个 `c`，1+12=13，严格等于文献“一十三级”，守恒律满足。
        *   *壸门合规校验*：`g[num=2](e1-g8)` 满足 `g[num=X](e[1-5]-g[8-12])` 格式约束。
        *   *首尾边界校验*：以 `Global(Sides=8,BaseR=7.5)+a` 开头，以 `-i` 结尾，符合刚性收尾要求。

*   **标准 LPSL 编码**：
FINAL LPSL: `Global(Sides=8,BaseR=7.5)+a-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-d[type=d1]-e[col=e1](e6-e12-e6/e9-e9-e9/e11-e8-e11)-f-g[num=2](e1-g8)-k-i`