**输入描述**：
目标古塔为昊天塔（大昊天寺塔）。据文献“六檐八角，高二百余尺”，判定该塔为**八角密檐式砖塔**，物理总檐数为6层。文献提及“栏槛缥缈”暗示可能存在勾栏结构，“熔铸丈六银佛...当塔之阳”指示塔身具有佛像（e10）装饰特征。

**推理过程**：
1. **第一步：类型与特征辨别**：
   文献明确指出“六檐八角”，结合辽金时期燕京地区高耸砖塔的营建法式（如天宁寺塔），判定昊天塔为典型的**密檐式塔**。其核心特征为：顶层特制塔檐，其下接续密檐循环，首层塔身承载主要装饰与造像。

2. **第二步：刚性覆盖与拓扑重组**：
   识别刚性数字特征：八角（`Sides=8`），六檐（`TotalLayers=6`）。
   基于亲缘原型伍侯塔的密檐骨架进行突变：保留顶层特制檐 `a`，后续需补齐5层标准密檐。由于是皇家敕建大塔（道宗皇帝、懿德皇后出资），斗栱铺作必不或缺，设定最底层（第6层）塔檐带有斗栱铺作 `d[type=d1]`，其余4层为标准叠涩与塔檐循环。由此生成檐区序列：`a-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-d[type=d1]`。

3. **第三步：柔性渗透与差异化采样**：
   - **塔身柔性渗透**：文献强调“欲于塔内熔铸丈六银佛...姑以金铜易像, 当塔之阳”，说明塔身向阳面必有佛像。设定塔身转角为普通柱 `[col=e1]`，单面墙体采用3x3网格化挂载，将佛像 `e10` 置于核心位置，结合假门 `e8`、假窗 `e11` 与华盖 `e9`，形成 `(e10-e8-e10/e9-e9-e9/e10-e11-e10)` 的组合，既符合辽金密檐塔首层装饰繁复的特征，又呼应了文献中的佛像记载。
   - **塔基柔性渗透**：文献提及“栏槛缥缈”，在塔基区引入勾栏 `o` 以体现登临远眺的缥缈之感；塔基设莲花座 `f`，壸门层设定为每面2个，间柱为普通柱，内雕人物（呼应皇家气派与佛教造像传统），即 `g[num=2](e1-g8)`。

4. **第四步：刚性拓扑约束验证链校验**：
   - **分界点判定**：序列中最后一个 `e` 为塔身，其左侧为檐区（无f/g/n），右侧为基座区（无a/c/r），划分清晰。
   - **层数守恒计算**：左侧檐区中，`Count(a)=1`，`Count(c)=5`，`Count(r)=0`，总和 1+5+0 = 6，严格等于文献物理总檐数“六檐”。
   - **壸门合规校验**：`g[num=2](e1-g8)` 严格满足 `g[num=X](e[1-5]-g[8-12])` 格式。
   - **首尾边界校验**：以 `Global(Sides=8,BaseR=10.0)+a` 开头，以 `-i` 收尾，符合绝对边界约束。

**标准 LPSL 编码**：
FINAL LPSL: `Global(Sides=8,BaseR=10.0)+a-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-d[type=d1]-e[col=e1](e10-e8-e10/e9-e9-e9/e10-e11-e10)-f-g[num=2](e1-g8)-o-i`