**输入描述**：
目标古塔为辽代大安八年所建的悟寂院舍利塔（白塔峪塔）。文献极度稀疏，仅明确其为“八角”形制及地宫内丰富的舍利、经咒供奉。结合辽代晚期“八角密檐式砖塔”的绝对主流形制（及实物现状），判定该塔为【密檐式塔】。

**推理过程**：
1. **第一步：类型与特征辨别**：文献虽仅提及“八角”与地宫舍利，但“大安八年”属辽代晚期，此时期八角密檐式砖塔为舍利塔的绝对标准制式。故判定为八角密檐式塔，物理总檐数依辽密檐典型规制设定为13层。
2. **第二步：刚性覆盖与拓扑重组**：提取刚性特征“八角”，映射为 `Sides=8`。基于亲缘原型永丰塔的密檐骨架，顶层特制檐为 `a`，后续12层标准密檐需通过循环算子补全垂直生长序列，底层檐下带斗栱 `d[type=d1]`，形成 `a` + 11次 `b-h-c` + 1次 `b-h-c-d[type=d1]` 的檐区拓扑。
3. **第三步：柔性渗透与差异化采样**：针对文献弱语义进行启发式推测。文献提及“镌诸杂陀罗尼造塔功德经”，暗示塔身具有强烈的经咒宗教氛围，故塔身转角柱由原型普通柱突变为经幢柱 `[col=e3]`；文献提及“九圣八明王八塔”及“石像数尊”，映射至塔身墙面装饰，采用双面交替的径向循环 `(e10-e14-e10)(e8-e9-e8)`（四面佛像莲台与四面假门华盖交替）；塔基壸门层则呼应“石像数尊”，设定为人物雕刻 `g8`，即 `g[num=2](e1-g8)`。
4. **第四步：刚性拓扑约束验证链校验**：
   - **分界点判定**：序列中最后一个 `e` 为塔身，其左侧为檐区（仅含a, c, b, h, d），右侧为基座区（含f, g, k, i），区域划分严格合规。
   - **层数守恒计算**：左侧檐区中 `Count(a)=1`，`Count(c)=12`，1+12=13，严格等于物理总檐数13层。
   - **壸门结构校验**：`g[num=2](e1-g8)` 严格满足 `g[num=X](e[1-5]-g[8-12])` 格式。
   - **首尾边界校验**：以 `Global(...)+a` 开头，以 `-i` 结尾，符合刚性收尾约束。

**标准 LPSL 编码**：
FINAL LPSL: Global(Sides=8,BaseR=6.0)+a-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-d[type=d1]-e[col=e3](e10-e14-e10)(e8-e9-e8)-f-g[num=2](e1-g8)-k-k-i