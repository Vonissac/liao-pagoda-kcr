**输入描述**：
目标古塔为昊天塔（大昊天寺塔），据文献记载为“六檐八角”，即平面为八角形、具有六层塔檐的砖塔。文献提及“栏槛缥缈”暗示存在勾栏结构，“欲于塔内熔铸丈六银佛...姑以金铜易像”暗示塔身与塔基具有丰富的佛像装饰。结合辽金皇家寺院砖塔的典型特征，判定该塔形制为**密檐式塔**。

**推理过程**：
1. **第一步：类型与特征辨别**：文献明确指出“六檐八角”，此为绝对刚性特征。塔型判定为密檐式塔。文献中“金铜标刹”为塔顶装饰，LPSL规范中塔刹不存不编码；“栏槛缥缈”对应勾栏代码 `o`；“金铜易像”对应佛像代码 `e10` 与 `g8`。
2. **第二步：刚性覆盖与拓扑重组**：基于刚性数字，平面边数 `Sides=8`，总檐数 `TotalLayers=6`。顶级构件序列需包含1个 `a` 和5个 `c`。参考亲缘原型伍侯塔的密檐骨架，重组为：顶层特制檐 `a`，其下首层密檐带斗栱 `b-h-c-d[type=d1]`，后续4层标准密檐循环 `b-h-c`。
3. **第三步：柔性渗透与差异化采样**：
   - **塔身柔性渗透**：皇家塔规格极高，转角柱采用龙柱 `[col=e2]`。结合“金铜易像”，塔身单面墙体采用3x3网格化分层：最上层飞天与佛像交替 `e6-e10-e6`，中层三华盖 `e9-e9-e9`，底层佛像夹假门 `e10-e8-e10`，组合为 `(e6-e10-e6/e9-e9-e9/e10-e8-e10)`。
   - **塔基柔性渗透**：塔身下方接莲花座 `f`。壸门层依皇家规制，设定每面2个壸门 `[num=2]`，间柱为龙柱 `e2`，内雕人物像 `g8`，结合为 `g[num=2](e2-g8)`。外侧呼应“栏槛缥缈”，挂载勾栏 `o`，最后收束于地表 `i`。
4. **第四步：刚性拓扑约束验证链校验**：
   - **分界点判定**：序列中最后一个 `e` 为塔身，其左侧均为檐区构件（`a,b,h,c,d`），右侧均为基座构件（`f,g,o,i`），区域划分合规。
   - **层数守恒计算**：最后一个 `e` 左侧统计，`a` 有1个，`c` 有5个，1+5=6，严格等于文献“六檐”，守恒定律满足。
   - **壸门结构合规**：`g[num=2](e2-g8)` 严格符合 `g[num=X](e[1-5]-g[8-12])` 格式。
   - **首尾边界刚性**：以 `Global(Sides=8,BaseR=7.5)+a` 开头，以 `-i` 收尾，完全闭合。

**标准 LPSL 编码**：
FINAL LPSL: `Global(Sides=8,BaseR=7.5)+a-b-h-c-d[type=d1]-b-h-c-b-h-c-b-h-c-b-h-c-e[col=e2](e6-e10-e6/e9-e9-e9/e10-e8-e10)-f-g[num=2](e2-g8)-o-i`