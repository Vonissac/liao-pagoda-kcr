* **输入描述**：
  目标古塔为朝阳南塔（灵感寺塔），据文献记载“凡一十三级，通百有余尺”，且提及“有百炼镜，以悬于外”。结合辽代营建法式与地理时空特征，判定该塔为【密檐式八角砖塔】。其核心特征为13层密檐、塔身带有佛像及假门装饰、基座具备壸门结构。

* **推理过程**：
  1. **第一步：类型与特征辨别**：文献明确指出“凡一十三级”，结合辽金时期该地域（柳城/朝阳）古塔的典型形制，判定为【密檐式塔】。文献中“百炼镜，以悬于外”暗示塔身或檐部可能有特殊装饰映射，需在柔性突变中予以体现。
  2. **第二步：刚性覆盖与拓扑重组**：识别绝对刚性数字为13层檐、八角平面。继承亲缘原型广胜寺塔的密檐骨架，配置 `Global(Sides=8,BaseR=8.0)`。顶层特制塔檐为 `a`，后续需补全12层标准密檐循环（采用 `b-h-c` 为核心递归单元），并在最底一层檐下加入斗栱 `d[type=d1]`，形成 `a + 12*(b-h-c) + d` 的垂直生长序列。
  3. **第三步：柔性渗透与差异化采样**：
     - **塔身突变**：针对“百炼镜，以悬于外”及辽塔常见形制，在核心分界点 `e` 的墙面网格中进行突变。转角柱采用普通柱 `[col=e1]`；单面墙体3x3图样设定为：顶层佛像-祥云（隐喻悬镜）-佛像 `(e10-e12-e10)`，中层三个华盖 `(e9-e9-e9)`，底层佛像-假门-佛像 `(e10-e8-e10)`，组合为 `(e10-e12-e10/e9-e9-e9/e10-e8-e10)`。
     - **塔基突变**：塔身下方接莲花座 `f`。壸门层设定为每面2个，间柱为普通柱 `e1`，内雕动物（呼应辽代游牧审美）`g9`，结合为 `g[num=2](e1-g9)`。外侧设单层栏板 `k`，最终过渡到地表 `i`。
  4. **第四步：刚性拓扑约束验证链校验**：
     - **分界点判定**：全序列最后一个 `e` 位于塔檐区之后、塔基区之前，位置合法，其左侧无 `f/g/n`，右侧无 `a/c/r`。
     - **层数守恒计算**：左侧塔檐区包含 1个 `a` 和 12个 `c`，1 + 12 = 13，严格等于文献“一十三级”，守恒律满足。
     - **壸门合规校验**：`g[num=2](e1-g9)` 严格符合 `g[num=X](e[1-5]-g[8-12])` 格式约束。
     - **首尾边界校验**：以 `Global(...)+a` 起始，以 `-i` 绝对收尾，满足刚性边界条件。

* **标准 LPSL 编码**：
FINAL LPSL:Global(Sides=8,BaseR=8.0)+a-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-d[type=d1]-e[col=e1](e10-e12-e10/e9-e9-e9/e10-e8-e10)-f-g[num=2](e1-g9)-k-i