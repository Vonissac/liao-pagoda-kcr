**输入描述**：
目标古塔为白塔峪塔（悟寂院舍利塔），建于辽大安八年（1092年）。文献明确指出其为“八角石藏”，结合辽代晚期觉华岛海云寺高僧营建背景及现存实物形制，判定该塔为【密檐式八角砖塔】。文献虽侧重地宫舍利供养（水晶塔、菩提树、石像等），但未直接描述上部结构，需依赖亲缘原型永丰塔进行密檐骨架的遗传与突变。

**推理过程**：
1. **类型与特征辨别**：
   文献载“八角石藏”，刚性特征为八角形（Sides=8）。结合辽代晚期密檐塔典型规制及实物遗存，该塔物理总檐数为13层。塔型判定为密檐式塔。

2. **刚性覆盖与拓扑重组**：
   以永丰塔密檐骨架为原型，执行递归算子赋值突变。顶层特制塔檐为 `a`；往下为12层标准密檐循环，采用 `b-h-c`（叠涩-素面-标准檐）作为核心重复单元；为体现辽代密檐塔底层檐下斗栱宏大的典型特征，最后一层密檐突变引入铺作层，变为 `b-h-c-d[type=d1]`。

3. **柔性渗透与差异化采样**：
   - **塔身装饰（柔性突变）**：文献提及“石像数尊”，暗示该塔具有浓厚的佛像供奉属性。塔身转角采用普通柱 `[col=e1]`。八个墙面采用双面交替模式（径向循环4次）：假门面内嵌佛像 `(e9-e8-e10)`，假窗面伴以祥云 `(e9-e11-e12)`，以此呼应地宫内的石像与宗教氛围。
   - **塔基结构（柔性突变）**：塔身之下接莲花座 `f` 与束腰 `b`。壸门层设定为每面2个，间柱为普通柱 `e1`，壸门内雕刻人物（呼应“石像数尊”） `g8`，组合为 `g[num=2](e1-g8)`。外侧辅以栏板 `k`，最终过渡至地表层 `i`。

4. **刚性拓扑约束验证链校验**：
   - **分界点判定**：序列中最后一个 `e` 为塔身，其左侧为塔檐区（无 f, g, n），其右侧为塔基区（无 a, c, r），区域划分合规。
   - **层数守恒计算**：左侧塔檐区中，`a` 的个数为 1，`c` 的个数为 12，总层数 = 1 + 12 = 13，严格等于物理总檐数，守恒校验通过。
   - **壸门结构合规**：`g[num=2](e1-g8)` 严格满足 `g[num=X](e[1-5]-g[8-12])` 格式约束。
   - **首尾边界校验**：以 `Global(Sides=8,BaseR=5.0)+a` 开头，以 `-i` 结尾，符合刚性收尾约束。

**标准 LPSL 编码**：
FINAL LPSL: Global(Sides=8,BaseR=5.0)+a-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-d[type=d1]-e[col=e1](e9-e8-e10)(e9-e11-e12)-f-b-g[num=2](e1-g8)-k-i