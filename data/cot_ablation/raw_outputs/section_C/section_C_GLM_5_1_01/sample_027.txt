**输入描述**：
目标古塔为白塔峪塔（悟寂院舍利塔）。文献《大安八年掩藏记》极度偏重地宫供养物描述（如水晶塔、银菩提树、石像等），未直接描述地上塔体结构。但文献首句“安置八角石藏于上”提供了关键的平面形制线索——八角形。结合辽代晚期觉华岛海云寺高僧背景及同区域同类塔形制，判定该塔为【密檐式八角砖塔】，物理总檐数参考亲缘原型推断为13层。

**推理过程**：
1. **【第一步：类型与特征辨别】**：文献提及“八角石藏”与“舍利塔”，结合辽金时期该地域（辽西走廊）高僧舍利塔的典型特征，判定为密檐式八角砖塔。总檐数依据刚性覆盖原则，采纳亲缘原型黄花滩塔的13层密檐特征。
2. **【第二步：刚性覆盖与拓扑重组】**：全局配置为 `Global(Sides=8,BaseR=7.5)`。塔檐区采用密檐式标准递归：顶层特制檐 `a`，其后紧跟12层密檐循环。前11层为标准叠涩与檐 `b-h-c`，第12层（最下层塔檐）加入斗栱铺作，突变为 `b-h-c-d[type=d1]`。
3. **【第三步：柔性渗透与差异化采样】**：
   - **塔身渗透**：文献中“造塔功德经九圣八明王八塔各名”及“石像数尊”暗示了浓厚的佛塔与造像崇拜。在黄花滩塔原型的3x3网格上执行突变：将上层中央的莲台 `e14` 替换为佛塔浮雕 `e15`（呼应“八塔各名”），保留两侧飞天 `e6`；中层保留三华盖 `e9`；底层保留佛像 `e10` 与假门 `e8`。形成 `(e6-e15-e6/e9-e9-e9/e10-e8-e10)`。转角柱采用普通柱 `[col=e1]`。
   - **塔基渗透**：文献强调“石像数尊”，故将原型壸门内的植物雕刻 `g10` 突变为人物雕刻 `g8`，形成 `g[num=2](e1-g8)`。保留双层栏板 `k-k` 与莲花座 `f`。
4. **【第四步：刚性拓扑约束验证链校验】**：
   - **分界点判定**：序列中最后一个 `e` 位于塔檐循环之后，其左侧仅有 `a, b, h, c, d`，右侧为 `f, g, k, i`，区域划分合法。
   - **层数守恒计算**：左侧塔檐区包含 1个 `a` 与 12个 `c`，1+12=13，严格等于物理总檐数13层，守恒成立。
   - **壸门结构合规**：`g[num=2](e1-g8)` 严格符合 `g[num=X](e[1-5]-g[8-12])` 格式。
   - **首尾边界刚性收尾**：以 `Global(...)+a` 开头，以 `-i` 结尾，符合绝对起点与终点约束。

**标准 LPSL 编码**：
FINAL LPSL: Global(Sides=8,BaseR=7.5)+a-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-d[type=d1]-e[col=e1](e6-e15-e6/e9-e9-e9/e10-e8-e10)-f-g[num=2](e1-g8)-k-k-i