**输入描述**：
目标古塔为朝阳南塔（灵感寺塔），据文献“凡一十三级”及辽代建塔历史背景，判定为**密檐式八角砖塔**。文献提及“百炼镜，以悬于外”，暗示塔身或塔檐可能有特殊的外部装饰映射（如假窗或铜镜位），塔基与塔身应具备辽代密檐塔的典型束腰与壸门结构。

**推理过程**：
1. **【第一步：类型与特征辨别】**：文献明确指出“凡一十三级”，结合朝阳地区辽塔的典型形制，判定该塔为十三层密檐式八角砖塔。基座半径参考同类辽塔取合理值8.0米。
2. **【第二步：刚性覆盖与拓扑重组】**：全局配置为 `Global(Sides=8,BaseR=8.0)`。十三层密檐的刚性结构为：1个顶层特制塔檐 `a`，加上12个标准塔檐 `c`。参考亲缘原型广胜寺塔的密檐循环骨架，采用叠涩束腰与标准檐的循环 `b-h-c` 进行11次重复，第12层檐下带有斗栱，变异为 `b-h-c-d[type=d1]`。
3. **【第三步：柔性渗透与差异化采样】**：
   - **塔身装饰突变**：原型塔身为飞天-莲台-飞天。针对文献中“百炼镜，以悬于外”的柔性特征，将中上层的莲台 `e14` 突变为假窗 `e11`（映射镜位），形成 `(e6-e11-e6/e9-e9-e9/e10-e8-e10)` 的3x3网格布局。
   - **塔基壸门突变**：原型为植物雕刻 `g10`。考虑到“火其砖，广若山枳”的素朴砖构感，将壸门内雕刻突变为素面 `g12`，间柱保持普通柱 `e1`，数量保持每面2个，即 `g[num=2](e1-g12)`。外侧保留单层栏板 `k` 作为过渡。
4. **【第四步：刚性拓扑约束验证链校验】**：
   - **分界点判定**：序列中最后一个 `e` 为塔身，其左侧全为塔檐构件（`a, b, h, c, d`），右侧全为塔基构件（`f, g, k, i`），区域划分合规。
   - **层数守恒计算**：左侧塔檐区包含 1个 `a` 和 12个 `c`，总和为13，严格等于文献“一十三级”，守恒公式成立。
   - **壸门结构合规**：`g[num=2](e1-g12)` 严格满足 `g[num=X](e[1-5]-g[8-12])` 格式。
   - **首尾边界校验**：以 `Global(...)+a` 开头，以 `-i` 绝对收尾，符合刚性边界约束。

**标准 LPSL 编码**：
FINAL LPSL: `Global(Sides=8,BaseR=8.0)+a-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-b-h-c-d[type=d1]-e[col=e1](e6-e11-e6/e9-e9-e9/e10-e8-e10)-f-g[num=2](e1-g12)-k-i`